# 文件格式转换网站 — 产品需求文档（PRD）

> 版本：v0.3.1（v0.3 定稿后审计补全 3.1 能力全景与 3.5–3.7 章节）　状态：已定稿　更新日期：2026-09-06

## 1. 项目概述

- **一句话定位**：公开免费的在线文件格式转换工具，免安装、浏览器即用。
- **目标用户**：普通用户，临时把图片 / 文档 / 音视频 / 压缩包从一种格式转成另一种。
- **核心价值**：
  1. 免安装、打开即用；
  2. 小文件本地转换，隐私优先、零服务器成本；
  3. 复杂格式服务端兜底，覆盖能力完整。

## 2. 完成标准（做到什么程度算「完成」）

| 维度 | 验收标准 |
|------|----------|
| 功能 | 四类格式的核心转换矩阵均可用，且支持本地优先的混合转换策略 |
| 安全 | 上传文件有类型/大小/魔数校验，临时文件转换完即删，无密钥泄露 |
| 性能 | 小文件本地转换 95% 场景 ≤ 10s 内完成；服务端转换有超时与队列 |
| 可用性 | 支持拖拽上传、进度展示、失败可重试；移动端可基本使用 |
| 成本 | 匿名与登录配额机制生效，能限制滥用与算力成本 |

## 3. 功能需求

### 3.1 转换能力全景

| 类别 | 互转矩阵（基线） | 操作能力 | 执行位置 | 范围 |
|------|------------------|----------|----------|------|
| 图片 | PNG/JPG/WebP/AVIF/GIF/SVG/BMP/ICO/TIFF/HEIC 互转；图片⇄PDF | 压缩（按质量/目标大小）、缩放/改分辨率、旋转/翻转/裁剪、加水印（文字/图片）、滤镜（灰度/锐化等）、EXIF 提取与删除 | 本地（Canvas + Wasm）为主；高分辨率/有损优化走服务端 | 本期：互转、压缩、缩放、旋转/裁剪、EXIF 删除；候选：TIFF/HEIC 进阶项 |
| 文档 | PDF⇄DOCX/XLSX/PPTX/MD/HTML/TXT/CSV（含 XLSX⇄CSV、MD→HTML 等） | PDF 合并/拆分/提取页/删除页/重排、PDF 压缩、PDF 加密/解密/加水印、文档→图片快照 | 服务端（LibreOffice/专用库）为主；MD/TXT/CSV 等纯文本可本地 | 本期：互转、PDF 合并/拆分/压缩；候选：PDF 加密/签名 |
| 音视频 | MP4/MKV/WebM/MOV/AVI；MP3/WAV/FLAC/AAC/OGG/M4A | 视频提取音轨、视频→GIF/抽帧、分辨率/码率/帧率调整、压缩体积、剪辑/裁剪/旋转/合并、字幕提取/添加/格式转换 | 小文件 ffmpeg.wasm 本地；大文件/复杂编码服务端 | 本期：互转、提取音轨、压缩、分辨率调整；候选：字幕、剪辑合并 |
| 压缩包/其他 | ZIP/TAR/TAR.GZ 互转；压缩/解压 | 加密压缩包、分卷 | 本地（fflate/JSZip），完全不上传 | 本期：互转、压缩/解压；候选：7Z/RAR、加密/分卷 |
| 进阶类别 | 电子书 EPUB/MOBI/AZW3⇄PDF；矢量 SVG/AI/EPS；3D OBJ/STL/GLTF/FBX；LaTeX→PDF | — | 服务端（Calibre/Inkscape/Assimp 等） | 候选（本期不做） |

- **音频转文字（ASR/语音识别）明确不做**（见第 6 节 Out of scope）：需 GPU/模型部署，与免费轻量成本模型冲突。
- 「格式 × 格式」完整可达矩阵以路由表形式在实现阶段落地（第 8 节待定项跟踪）。

### 3.2 转换方式：混合模式

- **本地优先**：判断文件大小与类型，满足条件则在浏览器端完成，**不上传**。
- **服务端兜底**：大文件或浏览器无法处理的复杂格式，上传到服务端用专业工具处理。
- **切换规则**：以「单文件大小 + 格式类别」为判定条件（阈值见 3.4 与 ARCH 路由表）。

### 3.3 用户系统

- **登录方式**：邮箱验证码登录（不存储密码）。
- **能力**：注册/登录、验证码下发与校验、登录态维持、配额查询。
- **匿名可用**：未登录也能用基础转换，但受更严格配额限制。

### 3.4 配额与限制（初始默认值，待最终确认）

| 类型 | 匿名 | 登录 |
|------|------|------|
| 单文件大小 | ≤ 50 MB | ≤ 200 MB |
| 每日转换次数 | 5 次/天 | 50 次/天 |
| 累计流量（每日上传体积总和） | 100 MB/天 | 2 GB/天 |
| 服务端文件保留 | 处理完即删 | 处理完即删 |

- 配额在服务端强制校验，前端仅做提示；本地转换计次不计流量。
- 达到上限后提示注册/登录或次日再试。

### 3.5 转换流程与交互

- **批量转换**：支持多文件同时选择，队列式逐个/并行处理。
- **结果下载**：单文件直接下载；批量任务完成后一键打包 ZIP 下载。
- **进度与取消**：服务端任务实时进度展示，运行中可取消。
- **失败重试**：失败任务保留原因说明，可一键重试。
- **输出命名**：结果 = 原文件名 + 新扩展名，重名自动追加序号。

### 3.6 合规与内容安全

- 用户协议与隐私政策页面，登录前勾选确认。
- 个人信息最小化收集：仅邮箱，不采集行为画像（个保法合规）。
- 内容安全审核：上传文件经审核服务检测，违规拦截并提示；审核服务异常按拒绝处理（fail-closed）。
- 版权免责：用户须对上传内容拥有合法权利；站点不留档、不存储。

### 3.7 国际化与增长

- **i18n**：zh/en 双语，locale 路由前缀切换，文案不硬编码。
- **SEO**：目标格式落地页（如 `/convert/png-to-jpg`）静态生成，覆盖长尾搜索词。

## 4. UI / 页面框架

| 页面 | 关键元素 |
|------|----------|
| 首页（转换） | 拖拽/点击上传区、格式选择、目标格式、开始按钮、进度条、结果下载、错误提示 |
| 登录 | 邮箱输入、验证码、发送/倒计时、协议勾选 |
| 配额/我的 | 剩余次数、剩余流量、当前套餐说明 |
| 目标格式落地页 | 「X 转 Y」说明 + 直接转换入口（SEO） |
| 合规页 | 用户协议、隐私政策 |

- 设计原则：极简、单页为王、大拖拽区、移动端适配。

## 5. 非功能需求

### 5.1 安全（底线）
- 所有服务端密钥（邮件服务、存储）走**环境变量**，不进前端、不进仓库。
- 上传文件做：类型白名单 + 大小校验 + **魔数（magic number）校验**，防伪造扩展名。
- 防 zip bomb、路径穿越、恶意上传；服务端转换进程做沙箱/超时/资源隔离。
- 邮箱验证码限流与防枚举；登录态、CSRF、HTTPS 完整。
- 转换完**立即删除**临时文件并清理临时目录；未下载文件定时兜底清扫。

### 5.2 性能
- 本地转换走 Web Worker / Wasm，不阻塞 UI。
- 服务端转换使用任务队列，限制并发与单任务超时。

### 5.3 可用性
- 转换失败给出明确原因并可重试；超时任务明确提示。
- 基础响应式布局，桌面为主要场景，移动端可用。

### 5.4 成本
- 配额机制限制匿名滥用；本地优先降低服务器算力消耗。
- 服务端扩容以「临时算力 + 即删存储」为主，无长期对象存储。

## 6. 项目边界

**In scope（本期做）**
- 见 3.1 中标记为「本期」的全部转换能力，外加混合转换、邮箱验证码登录、配额、处理完即删。
- 批量转换与打包下载、进度/取消/重试、输出命名、内容安全审核、用户协议与隐私政策、i18n、SEO 落地页（见 3.5–3.7）。

**Candidate（后续迭代）**
- OCR、电子书、矢量图（SVG 外）、3D 模型、LaTeX、7Z / RAR、视频字幕、PDF 数字签名。
- AI 增强 / 翻译（用户栈已有 DeepSeek/LangGraph 经验）。

**Out of scope（明确不做）**
- 音频转文字（ASR / 语音识别）。
- 付费订阅、支付、云盘、历史记录、批量文件夹、公开 API 开放平台。

## 7. 技术栈（已定）

- 前端：React
- 后端：Python + FastAPI
- 本地转换：浏览器 Wasm（ffmpeg.wasm / JSZip / Canvas / wasm 图像库）
- 服务端转换：ffmpeg / LibreOffice（或等价 CLI）以子进程或队列调用
- 数据存储：数据库（用于用户/配额，具体选型待架构阶段确定）

## 8. 待定事项

- [ ] 数据库选型（SQLite/PostgreSQL 等）
- [ ] 完整格式转换矩阵细化
- [ ] 邮件服务商选择
- [ ] 本地/服务端切换阈值最终数值
- [ ] 部署环境与域名